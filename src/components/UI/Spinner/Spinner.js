import React from 'react'
import classes from './Spinner.css'

const Spinner = (props) => {
    return (
        <div className={classes.Spinner}>Loadning....
        </div>
    )
}

export default Spinner

